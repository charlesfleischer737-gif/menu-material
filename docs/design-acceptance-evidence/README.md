**Design implementation evidence — September 17, 2026**

Candidate: working tree based on `657a2c6`, local development site at `http://localhost:5173`. These files document an evolving candidate, not a release or a completed acceptance run. Recheck evidence affected by later changes. No production deployment was performed.

Current source identity: **5ed62e23e15a**, recorded in [source-manifest.json](source-manifest.json). **95/153 applicable criteria and 1/9 shared gates have passed.** Two recovery criteria remain deferred by the user. The technical gate has passed with explicitly baselined lint errors; this is not overall design acceptance.

**Scope and status**

The active implementation goal covers all 30 recommendations. The user explicitly deferred account recovery (UX-29.5 and UX-29.6). The other 153 criteria and nine shared gates remain in scope. Actual Safari/Firefox, physical-device behavior, independent visual review, and participant sessions have not been verified. Human sessions must not be simulated or counted as passes.

The browser inventory identifies the test surface as **Codex In-app Browser**, browser 1/tab 2. Its engine version was not reported by that inventory. Earlier shorthand “Chromium” has been corrected to the actual surface name; this does not establish the full separate-browser matrix required by G-06.

**Implemented so far**

- Shared UI palette, typography, control sizes, working headers and gutters; pricing action visibility and unavailable-Pro copy.
- Persistent crop confirmation/download actions; grouped restaurant settings with unsaved-change handling and validation navigation.
- Tablet menu editing pane, desktop focus view, Fit page/width controls, compact phone toolbar and overflow actions.
- A shared mobile action footer that reserves its measured height and moves into document flow when it occupies too much of the viewport.
- Compact library filters, aligned card metadata, smaller missing-photo treatments, and removal of redundant desktop post-editing actions.
- Searchable Explore captions, compact style-browser filters and a persistent current-look summary.
- Compact signup with optional restaurant setup after credentials, tighter homepage emphasis and public reading layouts.
- Compact customer-menu selection and section-overflow navigation, including active-section reveal, search/menu reset behavior, and legacy-menu ARIA relationships.

Artwork contrast, operational/loading structures, inspector tabs, persistent sheet controls, and the 12-dish fixture are now implemented and partially verified. Comprehensive responsive/keyboard checks and remaining states still require work. Existing improvements are not an automatic acceptance pass.

**Captures**

| Files | View / fixture | Evidence limits |
| --- | --- | --- |
| `settings-details-phone.png`, `settings-look-phone.png`, `settings-ordering-phone.png`, `settings-save-failure-phone.png` | Isolated Juniper settings, 390 × 844 | Grouped layout, validation and rejected-save evidence. Current Saving and 503 captures are listed below; physical keyboard behavior remains unverified. |
| `library-phone.png` | Local three-dish collection, 390 × 844 | Compact default toolbar; does not cover the 12-dish stress fixture. |
| `menu-edit-tablet.png` | Earlier local dinner menu, tablet | Historical capture. Current tablet selection/edit/reload evidence is in the later captures and menu-responsive-measurements.json. |
| `menu-focus-desktop.png` | Juniper Dinner, 1280 × 720 | Refreshed focus-mode layout; preview region measures 1010px. |
| `menu-fit-page-phone.png` | Earlier local dinner menu, 390 × 844 | Historical Fit page capture. Current default Fit width evidence is menu-preview-phone.png and menu-responsive-measurements.json. |
| `post-action-bar-phone.png` | Existing local post, 390 × 844 | Shared action footer; artwork itself still needs the design/format stress review. |
| `photo-download-unchecked-phone.png`, `photo-download-checked-phone.png` | Existing local pasta photo, 390 × 844 | Confirmation and 48px Download action remain together; enabling does not move the footer. |
| `photo-download-complete-phone.png` | Same existing photo | UI reported download started and reviewed/saved. Browser download-event capture timed out; the actual file has **not** been verified. |
| `explore-current-desktop.jpg`, `explore-current-phone.jpg`, `explore-small-phone.jpg` | Full 56-style catalog | Current caption alignment and shared identity measurements are in style-discovery-verification.json. Older explore-desktop/phone.png are historical. |
| `style-browser-current-desktop.jpg`, `style-browser-current-phone.jpg` | Existing 56-style catalog | Four complete desktop cards and two phone cards, explicit actions and current-look footer. Older style-browser-desktop/phone.png are historical. |
| `auth-signup-phone.png` | Empty signup, 390 × 844 | Required credential inputs 44px high/16px text; primary action y=565.63–613.63, 48px high. No account submitted. |
| `auth-signup-small-phone.png` | Empty signup, 320 × 740 | No horizontal overflow; required inputs and submit visible. Long modal scrolls vertically. |
| `homepage-controls-desktop.png`, `homepage-controls-phone.png`, `homepage-controls-small-phone.png` | Current logged-out homepage | Shared action styling. Desktop comparison y=429.84px; 290.16px of image visible. Older homepage-desktop.png is historical. |

**Interaction observations**

- Explore empty search resets to 56 styles and search focus. The current lower-result walkthrough preserves the wood search, scrollY 235 and Natural daylight focus after returning from Studio.
- Style-browser categories, moods, four occasions, favorites and saved-look reuse/persistence have current evidence in the style-discovery section below. Physical touch and the full independent-browser matrix remain open.
- Crop confirmation toggled Download from disabled to enabled without footer movement. Current failure/retry and loading evidence is recorded below; actual browser-delivered file verification remains outstanding.
- The original pilot contains three dishes. A separate fictional Juniper fixture now contains five locally published menus, six sections per menu, 12 active dishes, two archived dishes, a 100-character name, four uploaded existing photos, two approvals, and extreme supported prices. No paid AI generation, purchase, real customer submission, or production publication was used.

**Verification already run**

The current candidate passed TypeScript, production build, the full application script (including studio once), and the complete menu suite after the menu status/selection and plan-dialog refinements. See [candidate-checks.json](candidate-checks.json) and its durable logs. Workspace exports passed 48 cases; [the log](checks/workspace-exports.log) covers supported compositions, paper profiles, carousel framing, searchable PDFs and 60-dish pagination. Earlier unaffected export tests (128 checks), template tests, and photo-preview tests (48 pixel comparisons) also passed; their execution scope is explicit in [checks.json](checks.json). The expanded artwork contrast run is recorded separately in post-artwork/contrast.json. The bundled Node runtime has no npm CLI: package scripts were expanded and the same test entry points/arguments executed in script order.

Full lint is **not clean**: 84 errors and 1,698 warnings. The current [diagnostic-level baseline](lint-diagnostic-baseline.json) compares complete error messages and code frames against HEAD, normalizing path/location formatting. Unchanged files match HEAD; modified-file errors have no unmatched diagnostics. Administration refinements removed eight prior errors. Warnings have not been individually baselined. No lint rule was disabled to report a pass. Older counts below document earlier candidates.

**Design pattern decisions**

- Application UI uses shared semantic colors, 8px controls and 12px panels. Customer-menu and exported-artwork palettes/fonts remain separate.
- Standard actions are at least 44px; primary task actions are 48px. The marketing CTA may use the documented 52px size.
- Desktop menu editing can use Focus preview; tablet editing switches to the selected item's controls. Phone actions share one measured footer.
- Explore retains the editorial serif heading as the explicit working-heading exception.
- Optional search filters collapse behind a counted Filters control. Search remains directly visible.
- Customer menus use a labeled native menu picker and horizontally scrollable section buttons with an explicit overflow action and current-section indicator.

The authoritative status list is [the acceptance tracker](../DESIGN_ACCEPTANCE_TRACKER_2026-09-17.md). Screenshots and source inspection are evidence for specific observations; they do not establish untested interactions or human usability outcomes.


**Latest measured and interaction evidence**

See [measurements.json](measurements.json) and [library-grid-measurements.json](library-grid-measurements.json). These are CSS-pixel observations at default zoom/text size, not physical-device results.

- Customer Dinner and Drinks begin at y=347.70 and y=374.89 at 390×844 with five menus and six sections. At 320px there is no page overflow; drink price columns remain aligned. The current candidate was rechecked at 390px and 320px: search for carrot gave one result, switching menus cleared stale search, section activation focused its heading and kept the active button visible in the rail, and the document had 0px horizontal overflow at 320px. The legacy renderer now exposes matching search/section relationships in source; a populated public legacy fixture remains unavailable. See [customer navigation verification](customer-navigation-verification.json) and the customer captures.
- More tools default actions now begin at y=202 desktop and y=298.21 phone, with 48px height; current evidence is in tools-verification.json. Staff upload was exercised empty, selected, invalid-image error, and valid replacement. Dish selection survived failure; replacement cleared the error. No valid staff photo was submitted. See staff-upload-empty-phone.png, staff-upload-selected-phone.png, staff-upload-error-phone.png, and staff-upload-empty-desktop.png. The incorrectly cropped selected-desktop capture was removed.
- Library default first card is y=260.41 at 390px. Name, price, and footer positions align exactly within measured rows at 1280px and 390px; maximum prices do not overflow. At 320px, cards use a readable 288px single-column fallback. See library-mixed-desktop.png, library-mixed-phone.png, and library-mixed-small-phone.png.
- No photo + Name A–Z returned eight dishes and persisted after closing/reopening Filters. Adding Show archived dishes produced two results and an explicit archived summary. Unmatched search gave zero and a reset. Search Espresso → Select → select its checkbox showed one selected; Done preserved the search and one result. See library-archived-phone.png, library-no-results-phone.png, library-selection-phone.png.
- The full 100-character dish name appears in its accessible detail heading. The missing-photo region is 96px high; name input y=431–479 and price y=524–572 are visible above a persistent footer beginning y=743.5. Close measures 44×44. See library-long-name-details-phone.png. Espresso price was edited to 3.50, saved, closed, and reopened: the value persisted and the card showed $3.50. Escape returned focus to Open Espresso; fresh filter dismissal returned focus to Filters.
- Post Maker and menu inspectors now use semantic tabs with matched labeled panels. ArrowRight selected/focused Design, End selected Caption, and Home returned to Details. Post tabs measure 44px high with 14px labels. Menu preview width is 510px at 1280px. Desktop duplicate editing controls are absent. Mobile editing has one Edit design entry and one persistent title/Close header; move/remove/close targets measure 44×44. See post-inspector-tabs-desktop.png, menu-inspector-tabs-desktop.png, and post-sheet-bottom-phone.png. The latter predates removal of a small gap above the sticky tabs and needs refresh.
- Incorrect local test-account password displayed a retryable error. Replacing only the password successfully opened the isolated restaurant, confirming email preservation. Required signup fields remain visible; optional restaurant setup follows credentials. See auth-login-error-phone.png and the refreshed auth-signup-phone.png. Software-keyboard behavior remains unverified.

**Artwork contrast and visual review**

[The contrast manifest](post-artwork/contrast.json) records 240 passing cases: ten designs, bright/dark/busy photos, v1/v2 compositions, Post, Story, and supported carousel slide kinds. It samples actual backgrounds under opaque glyph cores (excluding antialiased edges) at a 320px displayed width: 4.5:1 normal text and 3:1 large text. Photo-only designs have no text samples. Twenty additional 90-character headline renders passed artwork-boundary checks. This is contrast evidence, not full aesthetic or crop acceptance.

Contact sheets: [bright Post](post-artwork/bright-feed-contact-sheet.png), [bright Story](post-artwork/bright-story-contact-sheet.png), [dark Post](post-artwork/dark-feed-contact-sheet.png), [dark Story](post-artwork/dark-story-contact-sheet.png), [busy Post](post-artwork/busy-feed-contact-sheet.png), [busy Story](post-artwork/busy-story-contact-sheet.png). Representative full-resolution and 320px display artifacts are alongside them. These rendered files do not prove browser download completion. Finish all-design visual inspection, aspect-ratio/custom-setting stress cases, and browser preview/export comparisons.

**Additional shared conventions**

- Underline tabs switch substantial panels and support roving focus, arrows, Home/End, and labeled tabpanels. Filled pressed-button segments select compact modes such as Post/Story, Print/Phone, and account entry. Segments use 4px inset corners inside an 8px group. Navigation identifies the current destination.
- Shared dialogs/sheets restore focus to a connected, visible opener. The mobile editor has a persistent visible title/Close above one scrolling body. Library detail adds a persistent Save/status footer; fields lock during a pending save.
- Persistence vocabulary is shared: **Saving draft…**, **Draft saved**, **Draft not saved · try again**. It describes persistence only. A saved menu may also say **Live menu has an older version**. **Published** refers to the published revision. Photos retain **Processing**, **Needs review**, and **Approved / Ready to use** for their actual workflow states; approval does not mean publication. Existing progress/recovery messages remain factual.

**Menu and pricing verification added in the latest pass**

[Menu measurements](menu-responsive-measurements.json) cover seven food dishes, the two-size House lager, selection agreement, persistence and eighteen responsive widths. At 768 × 1024 and 1024 × 768, every tested food selection exposes its name and price without a discovery scroll. The inspector container receives focus, preserving deliberate text-field entry. Both drink price rows are visible at the tablet reference sizes. Full selected names now appear in the inspector heading and proof's accessible label.

- Sourdough was changed to “Sourdough & cultured butter — evening” and 9.25. Both values survived reload and appeared in the selected proof and outline. A pending 3.75 Half pint edit survived tablet → desktop → tablet while retaining input focus. These are isolated local fixtures, not restaurant production data.
- Resizing through 1250, 1100 and 1050px boundaries, down to 320px and up to 1440px, preserved the selected edit and produced no page-level overflow in the measured sequence. Default preview region is 510px at 1280; one action expands it to 1010px. Zoom in/out and Fit page/width were exercised with matching canvas/selection geometry.
- At 390 × 844, the default Fit width paper begins y=378.41px and is 332px wide. The first measurement exposed 364.59px; the refreshed capture exposes 368.59px above its clipping boundary. The persistent footer begins y=771px and is 73px high. Preview/zoom controls remain at least 44px. Current captures: menu-preview-phone.png, menu-edit-tablet-portrait.png, menu-edit-tablet-landscape.png, menu-selected-desktop.png, menu-focus-desktop.png and menu-fit-page-desktop.png.
- Drink size/price inputs now use 16px text and at least 44px height; remove actions are 44 × 44px. At 320px, option/price/remove widths are 92/88/44px with 8px gaps. A lone Price style field occupies the full 240px row and displays “Sizes / options” without truncation. See menu-drink-prices-small-phone.png and menu-drink-prices-tablet.png.
- The live-publication cue now accounts for pending local changes, rather than waiting for autosave to advance the saved revision. Lunch changed immediately to “Unsaved changes · Live menu has an older version,” then “Draft saved · Live menu has an older version.” An in-flight save of an older draft no longer briefly claims the newer draft is saved. Failure/retry UI still requires further verification.

[Plan measurements](plans-measurements.json) cover public pricing and the account dialog at both reference sizes. Free actions are 48px high, with 14px labels and 9.83:1 text contrast. Disabled Pro labels reach 5.07:1. Desktop action top/bottom coordinates match exactly in each context. The dialog now has one scrolling body and a persistent title/Close header; both actions fit in its initial desktop view, while Free is immediately visible on phone. After scrolling 582px through the phone dialog, the header and Close positions remain unchanged.

Keyboard Enter on Continue free dismisses the dialog. Enter on the public Try it free link opens the signed-in Photo Studio or guest Photo Studio according to session state. No purchase or paid generation was triggered. Availability comes from billingEnabled() through state/public pricing/account copy/workspace prompts/dialog. Unavailable Pro remains explicitly disabled, without an invented waitlist or notification route. Enabled-Pro fixture coverage remains open.

Reviewed captures: plans-dialog-desktop.png, plans-dialog-phone.png, plans-dialog-pro-phone.png, pricing-desktop.png, pricing-actions-desktop.png, pricing-phone.png, pricing-free-phone.png and pricing-pro-phone.png. Initial full-page browser captures had stitching discontinuities and were replaced with reliable viewport captures; those discarded composites are not acceptance evidence. Hover/pressed-state coverage is still open under UX-01.2.

**Public reading pages**

Privacy and guidelines now include a compact “On this page” navigation. Each link targets a focusable section heading. Native keyboard activation moves focus to that heading and shows the shared 2px focus outline. Settled sampled headings remain visible; the small-phone AI-processing target begins y=23.875px. Immediate focus measurements are explicitly distinguished from positions sampled after smooth scrolling finishes.

[Reading measurements](public-reading-measurements.json) cover both pages at 1280 × 720 and 390 × 844, plus privacy at 320 × 740. Titles measure 40px desktop and 28px phone. Body copy is 16px with 26.4px line height, a 62ch desktop measure and 16px phone gutters. Actual complete sample lines range from 66–78 characters, with median approximately 71. No page-level horizontal overflow was measured. Section boundaries use 32px separation and 24px padding; headings have 16px separation from their body copy. Inline and navigation links remain underlined.

All six privacy sections and four guideline sections preserve their baseline text; [the normalized-copy comparison](public-copy-preservation.json) records the matching hashes. The homepage and reading pages use the same Brand/footer-link treatment, with 45px-high information links. Footer navigation between privacy, guidelines, pricing and the homepage was exercised. Homepage copyright and original-photo license credits remain intact.

Current captures: privacy-desktop.png, privacy-phone.png, privacy-small-phone.png, privacy-anchor-desktop.png, privacy-anchor-small-phone.png, privacy-footer-phone.png, guidelines-desktop.png, guidelines-phone.png, guidelines-footer-desktop.png, homepage-footer-desktop.png and homepage-footer-phone.png. These were inspected at full size, including small-phone wrapping. Current typecheck, build and targeted public-page lint passed after these changes. See the public-reading entries in candidate-checks.json. Browser-native 200% text enlargement remains unverified; the available browser capability inventory exposes viewport size but no text-size or zoom control.

**Shared controls and restaurant settings**

The Button component now carries a stable data-ui-button marker, so components rendered with asChild retain the shared contract even when their semantic data-slot changes. This fixes the confirmation dialog's formerly 36px actions. Standard Button, cx-btn and md-button actions now share 14px/1.4 text, weight 600, an 8px radius and 8px icon gap; primary colors resolve from the root palette. The homepage hero and closing CTA explicitly use size=marketing (52px, 16px label, 24px horizontal padding). Header actions remain 44px; plan actions remain 48px. Primary and destructive actions define separate hover/pressed tokens. Equivalent outline states use the same canvas/selected fills. Browser hover/pressed coverage and the complete legacy-control inventory remain open under UX-07.

[Shared-control measurements](shared-controls-verification.json) record homepage, pricing and confirmation-dialog samples. The confirmation's two actions now measure 44px high with legible destructive/secondary colors. Keyboard focus is visible. Public primary actions use #174c3c rather than the former homepage-only near-black treatment. The header and footer brand links have at least a 44px hit height; mobile Before/After targets are also 44px. Uploaded artwork colors were not edited by these CSS changes.

[Settings verification](settings-verification.json) records direct group navigation, preserved edits, save/reopen, Keep editing, Discard, invalid URL/timezone navigation, a rejected save and a successful retry. The failure was produced by signing the isolated account out in a second local browser tab. The original form retained its brand text, reported the sign-in error and kept Unsaved changes. After the same fixture account signed back in, retry saved the unchanged form and cleared the error. This is a real session-expiry failure check, not coverage of every network/server failure. The subsequent response-gateway review below captures Saving and a 503 retry; UX-06.5 has now passed.

Settings has one body scroller. At 1280 × 720, it moved 1,265px while the header stayed y=25 and the footer y=590.5. At 390 × 844, it moved 1,234px while the header stayed y=0 and the footer y=747.5. Fields use 8px label spacing and 24px group gaps. Aligning field contents to the top fixes the currency/logo baseline mismatch. Weekly hours now use 44px time controls and readable borders; at 320px, the day label occupies its own row so the native time segments fit. The final control remains above the footer. Physical software-keyboard and safe-area behavior remains unverified.

Reviewed captures include settings-details-desktop/phone.png, settings-look-desktop/phone.png, settings-look-scrolled-desktop/phone.png, settings-ordering-desktop/phone.png, settings-hours-desktop/phone/small-phone.png, settings-discard-phone.png and settings-save-failure-phone.png. These use the isolated Juniper fixture. Saved local test values are cuisine “Seasonal American — acceptance review,” caption tone “Warm, concise, and welcoming,” ordering URL https://example.com/order and brand preference “Preserve this input through a failed save.” No menu was published by these checks.

**Homepage responsive review**

[Homepage verification](homepage-verification.json) covers 1280 × 720, 1440 × 900, 1024 × 768, 768 × 1024, 390 × 844 and 320 × 740. A decoration at 1024px brushed the copy column; side ornaments now move outward at intermediate desktop widths. Eight additional samples at 1000, 1001, 1099, 1100, 1101, 1200, 1259 and 1280px have no text-range intersections or page-level overflow. The metrics preserve the explicitly labeled pre-fix tablet sample. Block rectangles can include empty text margins; actual text ranges are the collision check used for acceptance.

The desktop comparison begins y=429.84px with 290.16px visible at the reference height. Phone Before/After selection works by pointer and in both arrow-key directions, with the correctly named image and visible focus. Actual touch-device verification remains open. Three use-case panels retain aligned desktop image, heading and description positions. All phone carousel cards were reviewed; its 44px previous/next controls disable correctly at their ends. The five-image offer matches public pricing. See homepage-use-cases-desktop.png, homepage-use-cases-phone.png, homepage-use-cases-menu-phone.png and homepage-comparison-keyboard-phone.png.

Current build and typecheck pass. The application regression suite passes again after the shared component change. A fresh lint capture still reports 92 errors and 1,699 warnings; the exact error comparison retains the existing manual account-panels context review and identifies no new errors. These results do not pass the unperformed browser/device/human gates.

**Photo finish and controlled failure review**

[Failure-state measurements](failure-state-verification.json) and [the local request log](response-gateway-events.jsonl) document controlled settings and photo-export requests through a temporary localhost:5174 gateway to the local preview on localhost:5173. The [gateway source](response-gateway.mjs) only matches explicit local method/path/query rules and logs no credentials or bodies. The rule profile was cleared and the temporary gateway stopped after review; the browser returned to localhost:5173 with its normal viewport. No paid generation, production changes or messages were involved.

Settings now has verified Saving, 503 failure and retry states. The Save button remains 133 × 44px while busy; controls and Close disable during the request. The failure preserves cuisine and Unsaved changes with an adjacent explanation; retry saves and clears the error. See settings-saving-phone.png and settings-server-error-phone.png. The local fixture now uses cuisine “Seasonal American” and brand preferences “Warm hospitality, natural materials, and clear seasonal menus.” Prior validation/session-expiry evidence remains applicable. Physical keyboard checks remain open.

Photo finish uses aligned 16px phone and 24px desktop gutters, 14px crop labels, and equal slider tracks. Both actions remain 48px high at 320px, including Preparing; measured x/y/width/height are identical before and during loading. The complete preview is contained inside its frame, with its destination caption visible. Desktop confirmation/actions stretch to the same edges as the preview; More photo actions follows the primary flow in the same desktop row and below it on phones. Its menu preserves all seven existing optional actions with 44px targets. Keyboard End reaches Apply to more dishes, and Escape restores the trigger. Closing the finish sheet restores Use photo focus.

At 390 × 844, the unchecked and checked footer starts at y=603.61 in both states. Confirmation, explanation and Download are simultaneously visible. At 320px, the last crop control can be fully scrolled above the footer, including with an export error. All six reference sizes and the 759/760/761px transitions have no page overflow. Final captures: photo-confirmation-unchecked/checked-phone.png, photo-crop-bottom-small-phone.jpg, photo-finish-desktop.jpg, photo-finish-tablet-portrait/landscape.jpg, photo-finish-large-desktop.jpg, photo-export-loading-phone/small-phone.jpg and photo-export-error-phone/small-phone.jpg. Quick adjustments and Post Maker crop controls were also visually checked at 320px after the shared slider change.

A forced 503 leaves the selected destination, confirmation and x51/y50/zoom1.02 crop intact. Retry reports a 912 × 912 export and the request log contains exactly one successful download request despite a repeated activation while preparing. Changing crop clears earlier success/error messages, resets confirmation and disables export until reviewed again. The browser download-event wait timed out, and no matching file was located in Downloads or the temporary directory. Therefore UX-02.5 remains in progress: UI success and rendering tests do not prove the actual delivered file. Native sharing and physical-device behavior remain unverified.

Final TypeScript/build pass. Studio/release checks, 48 crop pixel comparisons and 128 export checks pass with their logs in candidate-checks.json. Full lint still has 92 baseline errors and 1,699 warnings; the diagnostic comparison introduces no new errors. These checks pass UX-02.1–02.4 and UX-06.5 only; they do not close the remaining shared browser/device/human gates.

**Style discovery and saved collections — September 18**

[Style discovery measurements](style-discovery-verification.json) include the reproduced faults, final screenshots, UI walkthroughs and derived geometry assertions. Explore keeps its two-column gallery, with 16px visible names and categories. At 1280px its photos are about 488px wide; phone photos are 173px at 390px and 138px at 320px. All 28 paired rows have matching image edges and caption bottoms within 2px at the measured desktop/tablet/phone sizes. All 56 style names and preview paths match Photo Studio. Both surfaces use the existing catalog and search utility.

Explore now remains mounted when another workspace is visited, preserving its query. The selected tile and scroll position are restored when returning. The meaningful lower-result walkthrough searched wood, opened Natural daylight, selected it, and returned by keyboard: five results, query wood, scrollY 235 and Natural daylight focus were preserved. Escape from details restores the opener. No-result Show all styles restores all 56 results, search focus and the top of the page. This proves the automated keyboard/pointer path; UX-21.5 remains open for actual touch-device verification.

Explore details now have a persistent Use this look action and a separate scrolling content region. Desktop aligns the action beneath the description and the example caption beneath the photo; phone keeps the action visible as the final descriptive section is reached. Reviewed captures: explore-current-desktop/phone.jpg, explore-small-phone.jpg, explore-detail-desktop/phone/small-phone.jpg, explore-empty-small-phone.jpg and explore-return-context-phone.jpg.

The Studio browser gives each card an explicit 44px Use look or Selected action. At 1280 × 720, four complete cards end at y609.59 above footer y618; their images are 239px wide. At 390 × 844, two complete cards end at y557.15, above footer y718 even with a two-line current-look name; images are 172px wide. The final row clears the footer by 27.91px desktop, 21.98px phone and 21.95px at 320px. The summary remains visible during scrolling. Six reference-size screenshots and nine 759/760/761, 899/900/901 and 1099/1100/1101px measurements show no page overflow in the sampled catalog sequence. This does not substitute for the remaining whole-site/device gate.

All seven category filters return eight looks; All returns 56. Bright/Warm/Dark/Colorful return 23/26/17/18. Dark plus Fine Dining returns four with both applied values and a count of two visible after Filters closes. Reset and searches preserve the current selection. Favorites can be added, found in Saved, removed and added again. All four occasion collections open and return without changing the selection. Their desktop overview now fits four complete cards. Curated details use a complete contained preview and top-aligned description; the final controls are reachable above the footer. Reviewed captures include style-browser-current-desktop/phone.jpg, style-browser-bottom-desktop/phone/small-phone.jpg, style-browser-small-phone.jpg, style-browser-tablet-portrait/landscape.jpg, style-browser-large-desktop.jpg, style-filters-phone.jpg, style-occasions-desktop/phone.jpg, style-occasion-collection-desktop/phone.jpg and style-detail-desktop/phone.jpg.

The local fixture saved “Juniper seasonal dishes · warm wood and natural window light” from the Neighborhood table recipe with Warm wood selected. The long name wraps within the phone card. Searching the name finds one saved recipe while keeping the prior Clean & craveable selection; reusing it restores the saved name and Warm wood. The recipe and Natural daylight favorite persist after reload. Archive → Archived → Restore also works at 320px. The recipe remains active; no default or image allowance was changed. See style-saved-custom-phone.jpg, style-saved-reused-phone.jpg, style-saved-reloaded-phone.jpg and style-saved-archived-small-phone.jpg.

A temporary local GET /api/studio-library 503 displays a clear collection-unavailable state and retry action. During a controlled ten-second retry the dialog says Loading saved looks rather than claiming the collection is empty. Actual retry returns the saved recipe and favorite. Error, loading and loaded samples have identical body/footer rectangles and preserve Polish my original as the current look. See style-saved-load-error-phone.jpg, style-saved-loading-phone.jpg and the response-gateway request log. The profile was cleared, gateway stopped, and browser returned to localhost:5173.

Current typecheck, build and full application regression pass. Full lint remains at 92 existing errors and 1,699 warnings, with no introduced error diagnostics after baseline comparison. UX-21.1–21.4 and UX-22.1–22.5 are passed; touch, enlarged text, distinct browsers, independent visual review and actual participant sessions remain open under their existing criteria/gates. No deployment occurred.

**More tools, activity and import persistence — September 18**

[Tools verification](tools-verification.json) contains screenshots, measured layouts, UI walkthroughs and derived assertions. UX-24.1, UX-24.3 and UX-24.4 are newly passed; UX-24.2 has refreshed measurements. The page uses one compact More tools header, five 48px desktop tabs and a labeled 48px native phone selector. The default Enter manually action starts at y202px desktop and y298.21px phone. ArrowRight, End and Home move the selected task and focus together. A pending import description survives desktop → phone → desktop while still Saving draft; batch target Rigatoni and its selected checkbox survive task navigation and resizing.

The batch uploader now precedes an expandable, complete list of 12 dishes needing approved photos. Desktop upload controls share y339.20 and 48px height; phone fields stack cleanly. The three weekly suggestion cards align all six content/action rows exactly in the measured desktop view; narrow cards wrap names, prices and dates without overflow. Edit this promotion opens the matching Rigatoni for lunch draft, and the separate Campaigns header action reaches the existing editor. The fixture uses deliberately extreme existing prices. No photos were generated or promotions published. Reviewed captures: tools-import-desktop/phone.jpg, tools-batch-desktop/phone/small-phone.jpg, tools-staff-desktop/phone.jpg, tools-weekly-desktop/phone/card-phone/small-phone.jpg and tools-activity-desktop/phone.jpg.

Import description fields now allow readable multiline editing. Manual drafts do not offer reading an absent menu file. Autosaves serialize revisions and only report saved when the latest revision has finished. In the exact-boundary test, the first request had completed and the next had been received while the UI correctly retained Saving draft. A controlled 503 preserved the full description. Save draft retry disabled actions, issued one request despite repeat activation, succeeded and reopened the latest description from the server. The manual import remains unreviewed and has not added dishes or published anything. See tools-import-overlap-phone.jpg for the persistence observation; it predates the final footer and is not current layout evidence.

Save status, review confirmation and import actions now share the responsive action footer. Failure, pending and success have identical footer and button rectangles at 390px; actions remain 48px high. At 320px the longer primary label wraps, both buttons share the same 59.19px height, and the footer ends at y740. Duplicate bottom padding/footer space was removed only when this fixed import footer is active. Final row controls remain reachable above it. Desktop keeps the footer within the form and caps the action pair at 480px. Reviewed captures: tools-import-edit-desktop.jpg, tools-import-small-phone.jpg, tools-import-error-phone.jpg and tools-import-saving-phone.jpg.

Activity retains its header, refresh action and four metric panels throughout initial loading and refresh. Initial-to-loaded header/navigation/metric movement is 0px; the action width changes by only 0.33px. Refresh failure retains prior values and exposes Retry. A delayed retry disables repeat activation and produces one request; the actual response restores the up-to-date status. A controlled zero-value response displays a useful empty state, and its Open Photo Studio link was followed successfully. Export actions remain clearly distinguished from file receipt or publication. Reviewed captures: tools-activity-loading-phone.jpg, tools-activity-error-phone.jpg, tools-activity-refreshing-phone.jpg and tools-activity-empty-phone.jpg. The local response profile was cleared, gateway stopped and browser returned to localhost:5173; request evidence is in response-gateway-events.jsonl.

The isolated fixture now has opening hours 11:00–21:00 on all seven days and one unapproved promotion draft from the suggestion walkthrough. No production state changed. The wrongly labeled tools-weekly-validation-phone.jpg is historical and excluded from proof; its capture followed a hot reload to Import. Similarly, a late overlap sample is excluded in favor of the exact request-boundary observation. These exclusions are explicit in tools-verification.json.

Current typecheck, build and application tests pass. Lint remains at 92 pre-existing errors and 1,699 warnings, with no introduced error diagnostics. UX-24.5 remains open for remaining staff review/batch progress states and the older Campaigns editor's shared patterns. Activity and import results are partial evidence for UX-30, not whole-site acceptance. At that earlier checkpoint, administration, physical devices, independent browser checks and the required human reviews remained open. Administration evidence follows below. No deployment occurred.

**Administration layout and recovery states — September 18**

[Administration verification](admin-verification.json) records measured views, role checks, local request observations and exclusions. UX-30.5 passes. Section headings, labeled 44px controls, grouped report actions and consistent panel spacing now follow the shared interface patterns. On desktop, budget, allowance and Save controls have exactly matching bottoms; Support minutes and Log time align exactly. Forms stack at narrow widths. Populated, long-report, availability, empty and failure layouts show no page overflow in the sampled 320–1440px views, including 1099/1100/1101px. This is criterion-specific evidence, not the complete device matrix.

The initial administrative skeleton reserves operational panels. At 390px, loading-to-loaded header, H1, navigation and 128 × 44px Refresh movement is 0px. A delayed refresh keeps existing information visible. A 503 shows a local error and Retry while preserving the typed invitation email; three screenshots document entry, failure and recovery because this browser accessor does not expose email values. No invitation or reset link was created. Initial failure uses one Retry action in the stable header. Reviewed captures include admin-current-desktop/phone.jpg, admin-small-phone.jpg, admin-loading-phone.jpg, admin-refreshing-phone.jpg, admin-refresh-error-phone.jpg, admin-initial-error-small-phone/phone/desktop.jpg and admin-pending-invite-before-refresh/after-error/after-retry-phone.jpg.

Restaurant saves reserve a status slot beside their controls, disable repeat actions while pending, and retain values after failure. The isolated Juniper budget successfully changed 20 → 21 through retry and was restored to 20. The delayed retry issued one POST, and pending-to-success button movement is 0px. The final failure has a local alert without a duplicate global toast; reverting the input clears Unsaved changes. Reviewed captures: admin-restaurant-desktop/phone.jpg, admin-restaurant-save-error-phone.jpg and admin-restaurant-saving-phone.jpg.

Photo progress keeps the previous report visible during a same-mode refresh, identifies failure separately, and retries through one request. Its refresh button retains identical geometry through failure, pending and success; the selected production mode remains unchanged. The original measurement explanation is available below the report. Availability supports pilot selection, search and a useful no-match state; reverting to open clears its unsaved status. All 65 current/legacy administrative look options remain available. No availability setting was saved. Reviewed captures: admin-progress-desktop/phone.jpg, admin-progress-error/loading-phone.jpg, admin-availability-small-phone.jpg and admin-look-search-empty-small-phone.jpg.

Account access and the long controlled food report use aligned fields and grouped actions. Report image links now use shared 44px controls. No report response, resolution, image restoration or support-time entry was submitted. Empty restaurants and events have explicit messages. Reviewed captures: admin-account-access-desktop/phone.jpg, admin-report-desktop/phone.jpg and admin-empty-small-phone.jpg. The report screenshots retain earlier in-memory unsaved copy; this limitation is recorded rather than used as evidence for final feedback wording.

[Role checks](admin-role-checks.json) verify that administrator reads succeed, anonymous/owner administrative reads are forbidden, and an owner administrative mutation is rejected. Broader capability/state coverage remains open under UX-30.6. Current typecheck, production build and full application tests pass. Full lint has 84 baseline errors and 1,699 warnings with no unmatched error diagnostics. The response profile was cleared, gateway stopped and browser returned to localhost:5173. Recovery integration remains deferred; no deployment occurred.

**Campaigns, draft integrity and preview recovery — September 18**

[Campaigns verification](campaign-verification.json) records this phase. Campaigns now uses the shared H1, a compact saved-campaign selector, consistent 44px/16px fields and semantic section headings. Desktop offer/review headings and first field edges align exactly. Narrow layouts provide keyboard-operable Review package and Back to offer jumps that focus the matching heading; at 320px those headings begin around y133px and y130px below the fixed navigation. All six existing export formats remain available in one labeled selector. Restaurant artwork and export rules remain unchanged.

A fresh-load overflow exposed a fieldset/grid sizing problem missed by resize-only checks. A separate form wrapper and grid with zero-minimum tracks fix it. Nine fresh-document widths (320, 390, 768, 1024, 1099, 1100, 1101, 1280 and 1440px) show no page overflow. The final action row gives Download and Copy caption matching 61.19px heights at 320px; Approve and Publish are 48px and Open hosted menu is 44px. Tablet preview width is capped at 520px. Reviewed captures: campaign-current-1280/1024/768/390/320.jpg, campaign-review-phone/small-phone/tablet.jpg and campaign-actions-phone/small-phone.jpg.

Saving now serializes requests and reports Draft saved only when the current content is persisted. At the exact overlap boundary, two requests had been received and one completed while the newest description remained visible with Saving draft. A 503 preserves the description and exposes Retry save. Its failure/pending button geometry has 0px movement in document coordinates; retry performs one request, disables form controls, succeeds and reopens from a fresh document. Pending text survives 1280 → 390 → 320 → 1280 resizing. Saving, approval and operation feedback use separate messages. Reviewed captures: campaign-save-error-phone.jpg and campaign-saving-phone.jpg.

Five format previews render successfully with the existing fixture photo. DoorDash correctly reports the existing minimum-image requirement; the 220px error region fits at 320px and Choose a photo focuses Food photo. A temporary unapproved copy of an existing photo forces an actual image 503. Retry performs one successful request, reports Preparing preview, and renders within an unchanged 405px reserved canvas. Error, pending and success document rectangles match exactly. Reviewed captures: campaign-preview-size-error-small-phone.jpg, campaign-preview-error-phone.jpg, campaign-preview-loading-phone.jpg and campaign-preview-ready-phone.jpg. The temporary asset is removed and the original photo/description restored; [fixture verification](campaign-fixture-restored.json) confirms 12 active dishes, allowance 5, and an unapproved/unpublished campaign.

A routing defect also caused saved campaign links to reopen More tools. The resolver now recognizes the promotion path and preserves its identifier; fresh-document checks and the workspace-navigation regression pass. Current typecheck, build and application suite pass. Lint has 84 baseline errors and 1,698 warnings with no unmatched errors. The gateway is stopped and the normal local preview restored. This evidence advances UX-24.5 and shared state verification without prematurely passing the remaining staff review, batch-progress, full workflow, browser/device or human gates. The total remains 79/153 criteria and 1/9 gates. No deployment occurred.

**Staff photo review, batch progress and upload recovery — September 18**

[Staff/batch verification](staff-batch-verification.json) closes UX-24.5 together with the earlier tools and Campaigns records. Staff review now shows the entire photo using contain-fit images, specific accessible action names, 48px approval/rejection controls and aligned card rows. Three desktop cards with one-, two- and four-line titles align their actions exactly. A checked confirmation survives a 503 and resizing; pending controls lock without moving the action. A single retry approves the local photo and focuses the remaining-review heading. Rejection failure, successful rejection followed by refresh failure, Refresh list retry and the empty state were exercised. A saved review is clearly distinguished from a stale list. Reviewed captures include staff-review-cards-desktop.jpg, staff-review-pending-phone.jpg, staff-review-error-phone.jpg, staff-review-reject-error-phone.jpg, staff-review-refresh-error-phone.jpg and staff-review-empty-phone.jpg.

Batch progress translates internal statuses into readable waiting, creating, partial-success, failed and ready states. Finished photos remain reviewable while unsuccessful images retry. Exhausted retries expose Open dish with an explanation. Phone action pairs share their height, and feedback appears below them; error-to-pending action movement is 0px. At 320px the pair becomes two full-width 44px actions. Fresh layouts at 320/390/768/800/801/1024/1280/1440px and 359/360/361px boundary checks fit without horizontal page scrolling. Upload/select controls measure 48px. Five chosen dishes survive create failure and pending resizing; the successful controlled request clears selection. Reviewed captures include batch-progress-final-desktop.jpg, batch-progress-error-phone.jpg, batch-progress-pending-phone.jpg and batch-progress-final-small-phone.jpg.

Real local upload checks cover pending locks, two successes, a partial result, selecting the same failed file again, and rejection of six files. The partial result preserves the successful photo and directs the user to retry only the unsuccessful file. That retry submits once and succeeds. The file input resets so same-file selection works. Upload feedback now reflects actual outcomes, and the generation estimate matches the existing one-image-per-dish request. Reviewed captures: batch-upload-partial-phone.jpg and batch-upload-validation-small-phone.jpg. Shared operation handling was extracted without changing administrative behavior.

[Fixture verification](staff-fixture-restored.json) confirms the three temporary staff assets and four source uploads were removed. The four original assets, two approved assets, 12 active dishes and five remaining images are preserved; no batch jobs were created. Generation UI requests and staff-link revocation UI used controlled responses; actual server semantics passed the existing stubbed regression suite. Current typecheck, build and full application tests pass. Lint remains 84 baseline errors and 1,698 warnings with no unmatched error diagnostics. The gateway is stopped, localhost:5173 is restored, and nothing was deployed. The total is now **80/153 applicable criteria and 1/9 shared gates passed**; two recovery criteria remain deferred.

**Shared working headers and menu commands — September 18**

[Shared-interface verification](shared-interface-verification.json) closes UX-05.1–05.3 and UX-08.1–08.4. The [desktop](shared-header-desktop.html) and [phone](shared-header-phone.html) contact sheets compare all six actual workspace headers with expandable matched earlier captures. Standard headings are 30 px desktop and 26 px phone in a 44 px row. Title/content edges match exactly at x 244 desktop and x 16 phone. The status sits 4 px below the title. Photo Studio's phone header is 20.20 px shorter while revealing its previously clipped save status. Explore remains the documented editorial serif exception. An 89-character menu name wraps at 1280/390/320 px without losing actions; the chevron keeps its 18 px size. The fixture name was restored.

The [contrast table](shared-menu-contrast.md) includes actual default, hovered, selected, unavailable and destructive commands. Add dish, quiet outline commands, inspector actions and Explore designs now have 44 px targets and 14 px labels. Selected outline rows include a left bar and aria-pressed. A nested selected price exposed a 4.25:1 legacy color; it now measures 8.37:1. Save/remove actions align exactly with an 8 px gap. Enabled zoom commands use a white grouped surface, while disabled controls keep the unavailable surface; both remain legible. The proof selection uses shared colors and a white outer ring without changing exported artwork. Photo Studio's upload action now uses the shared 8 px radius.

Phone geometry still passes: actual food proof begins at y 379.21 with 367.79 px visible after accounting for the scroll-container clip; the first library card begins at y 271.21. Tablet selection retains visible name/price fields at 768×1024 and 1024×768; sampled 320/390 px editor layouts do not overflow. Current typecheck, final build and full application+menu tests pass. Lint remains 84 baseline errors and 1,698 warnings with no unmatched errors. No deployment was performed. The total is **87/153 applicable criteria and 1/9 shared gates passed**; two recovery criteria remain deferred. UX-08.5 and all actual enlarged-text/device/independent-review gates remain open.

**Shared controls, responsive focus and account pending states — September 18**

[Control verification](controls-verification.json) closes UX-16.3, UX-16.4 and UX-17.4. Menu Studio, Post Maker and More tools retain native tab semantics, selected-panel relationships and arrow/Home/End navigation. Tab advances to the visible panel. Post/Story, Print/Phone, account modes and mobile menu views expose pressed states and activate by keyboard. Current workspace destinations include More tools and Administration; selected secondary destinations have a visible left marker. More tools panels now show a keyboard focus outline.

An actively used Post Maker inspector stays open when it becomes a phone sheet, preserves the selected panel and design values, and focuses the selected desktop tab when widened. Tests at 760/761px, 390px and 320px exposed and repaired a stale sheet reopening over Menus. Leaving Post Maker now hides its portal; returning and closing it focuses Edit design. More tools moves focus between its desktop tabs and native phone selector without changing the selected tool. The [capture contact sheet](controls-contact-sheet.html) includes phone, small-phone, desktop and boundary views.

Account mode buttons, credential fields, optional setup, submit and Close lock during pending sign-in and use visible unavailable states. Three [controlled local requests](controls-auth-events.json) returned delayed 503 responses without reaching the authentication server. Error screenshots retain the entered test email and masked password. The browser value accessor returned empty strings, so no value-preservation claim relies on that accessor. Closing sign-in from mobile navigation now returns to Open navigation, or Log in after widening. Recovery integration remains deferred.

Current typecheck, production build and application+menu tests pass. Lint remains 84 existing errors and 1,698 warnings with no unmatched errors. The test gateway is stopped and the normal local preview restored. The total is **90/153 applicable criteria and 1/9 shared gates passed**. UX-16.2's complete visual state matrix, enlarged text, actual software-keyboard/device coverage and independent human acceptance remain open. The 36px mobile workspace-options target is explicitly retained as remaining UX-04 work. No deployment occurred.

**Icon targets, style-card actions and compact navigation — September 18**

[Target verification](target-verification.json) closes UX-11.3 and UX-11.4. All five destinations were activated at 320px and 390px. Targets are 56px high, labels remain on one line, and the active route combines aria-current with 700 weight. The whole phone header remains 105px high. Plans, settings, More tools and Sign out stay in the separate utility menu; native Tab shows a visible focus ring on plans. The desktop sidebar now fits all actions within 720px. The [capture contact sheet](targets-contact-sheet.html) shows the actual layouts.

Settings Close no longer shrinks below 44px at 320px. The mobile workspace-options target is 44px; its padded edge opens the menu. Menu reorder controls retain 44px targets with 8px between them. Size-option removal now announces both index and label; native Tab scrolling keeps its focus ring above the fixed footer. Menu disclosures are 44px high with 14px text. Public enlargement/account close, Explore close, customer section navigation and the other measured families retain 44px targets. Complete icon-family and button-state coverage remains open under UX-04 and UX-07.

Style favorites now sit beside Use look below each photograph, preventing overlapping image/favorite hit areas. Both actions are 44px high with an 8px gap, and the heart stays 16px. All 56 cards were measured at 320,390,768,1024 and 1280px. Fresh initial rows still show four complete cards on desktop and two on a phone. Final rows clear the footer by 27.91px desktop, 21.98px phone and 22.45px small phone; the current-look summary stays visible. The temporary favorite was restored and the current look preserved. UX-22's affected visual evidence is refreshed.

Typecheck, production build and the full application+menu suites pass. Lint remains 84 baseline errors and 1698 warnings with no unmatched errors. The evidence distinguishes transient measurements, hidden-input classification, incomplete selectors and actual native keyboard results; none of those limitations are counted as passes. The original Dinner selection and local preview are restored, viewport override reset, and no deployment occurred. **92/153 applicable criteria and 1/9 shared gates have passed**; two recovery criteria remain deferred. Actual enlarged text, other browsers/devices and independent human review remain open.

**Public and portal palette, gallery controls and plan focus — September 18**

[Palette verification](button-palette-verification.json) closes UX-09.2. The [role inventory and contrast table](interface-palette.md) documents the single semantic palette now defined in globals.css. Public/portal defaults no longer retain separate cream, ink or error values. Success and loading have named roles. Primary pricing actions resolve identically outside the workspace and inside its portal: 48px height, 14px/600 type, 8px radius/gap and matching default, hover and focus colors. The workspace primary matches; public standard and marketing actions remain 44px and 52px. Disabled Pro remains unavailable.

Dialog defaults now use the shared white surface, ink, border, 12px corners and restrained shadow. Full-screen settings and bottom-sheet corner exceptions remain intact. The homepage photo dialog and selector no longer use a separate cream/olive/brown palette; selected radios have a checkmark and border, and keyboard focus uses the shared green. The enlargement button adds a white inner frame so focus remains visible over the image. The original example was restored after arrow-key verification. See the [actual capture contact sheet](palette-contact-sheet.html).

Resizing Plans exposed a lost-focus dismissal. Closing now focuses the visible phone utility trigger or desktop Plans button, with both directions verified. Fresh native Tab traversal reaches the public Free action within the phone viewport, and Enter opens the guest studio. Earlier offscreen locator-focus evidence is explicitly superseded, not treated as a pass. Held pressed states, full button-family loading/enlargement checks, remaining color-role inventory, custom-artwork preservation and the independent browser/device/human gates remain open.

Final typecheck, build and full application+menu tests pass. Lint remains 84 baseline errors and 1698 warnings with no unmatched errors. Recovery is deferred; no account/restaurant data, billing, generation or publishing operation was performed. The local preview and normal viewport are restored. **93/153 applicable criteria and 1/9 shared gates have passed**; nothing was deployed.

**Remaining independent work**

Complete the remaining reference-page/dialog matrix, enlarged text, loading-to-loaded geometry, and representative save/error/retry states. Menu editing's tablet/breakpoint sequence is covered above; other workspaces still need their prescribed additional widths. Complete remaining operational capability and role-state coverage outside More tools. More tools and Campaigns shared patterns are recorded above. Explore and style-browser captures are refreshed above. Post-sheet focus and current narrow-layout captures are refreshed above; administration desktop and phone evidence is recorded above. Exercise populated legacy customer navigation and verify an actual browser-downloaded file. Preserve every unverified item in the tracker. Actual Safari/Firefox and phone-device coverage plus the specified independent visual and participant reviews remain required. No recruitment, outbound messages, deployment, or certification is implied.
